name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      # Same minor as the golang base images, so CI tests the toolchain that ships.
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.27"
          check-latest: true
          cache: true
      - run: go mod download
      - name: Build
        run: make compile
      - name: Vet and gofmt
        run: make lint
      - name: Staticcheck
        run: make staticcheck
      - name: Reachable Go vulnerabilities
        run: make vulncheck
      - name: Validate Compose files
        run: make validate-compose

  # The slowest check, split out so it runs beside the others instead of after them.
  go-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.27"
          check-latest: true
          cache: true
      - name: Test
        run: make test-race

  web:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 26
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: make web-install
      - name: Audit dependencies
        run: make web-audit
      - name: Build
        run: make web-build

  # Without this, a Dockerfile break first surfaces during a release. The crawler is covered by
  # manifest-merge, which builds both of its platforms.
  images:
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        component: [api, web]
        arch: [amd64, arm64]
        include:
          - component: api
            dockerfile: Dockerfile.api
          - component: web
            dockerfile: web/Dockerfile
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1
      - name: Build ${{ matrix.component }} image for linux/${{ matrix.arch }}
        uses: docker/build-push-action@c3c9e263c25d99ce0380d002d59b67737d91b0dc # v7.4.0
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: false
          platforms: linux/${{ matrix.arch }}
          cache-from: type=gha,scope=image-${{ matrix.component }}-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=image-${{ matrix.component }}-${{ matrix.arch }}

  # Without this, a break in the release's manifest assembly first surfaces during a release.
  # Crawler only: it cross-compiles, so one runner covers both platforms without QEMU.
  manifest-merge:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      IMAGE: localhost:5000/enrscout-crawler
    services:
      registry:
        image: registry:2
        ports:
          - 5000:5000
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: docker/setup-buildx-action@f87e5991a6d7451dcb8d9637bfbc97413f497069 # v4.4.1
        with:
          driver-opts: network=host
          buildkitd-config-inline: |
            [registry."localhost:5000"]
              http = true
      - name: Build both platforms by digest
        run: |
          mkdir -p "$RUNNER_TEMP/digests"
          for platform in linux/amd64 linux/arm64; do
            docker buildx build --platform "$platform" -f Dockerfile.crawler \
              --provenance=true --sbom=true \
              --output "type=image,name=${IMAGE},push-by-digest=true,name-canonical=true,push=true" \
              --metadata-file "$RUNNER_TEMP/meta.json" \
              --cache-from type=gha,scope=ci-manifest-merge \
              --cache-to type=gha,mode=max,scope=ci-manifest-merge .
            digest="$(jq -r '."containerimage.digest"' "$RUNNER_TEMP/meta.json")"
            touch "$RUNNER_TEMP/digests/${digest#sha256:}"
          done
      - name: Merge and validate
        run: |
          digest="$(.github/scripts/merge-manifest.sh "$IMAGE" "$RUNNER_TEMP/digests" "${IMAGE}:ci")"
          echo "merged ${IMAGE}@${digest}"

  dependency-review:
    # Dependency review requires GitHub Code Security on a private repository.
    if: github.event_name == 'pull_request' && github.event.repository.visibility == 'public'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0

  # The single required status check: matrix job names embed matrix values.
  gate:
    if: always()
    needs: [go, go-test, web, images, manifest-merge, dependency-review]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require every upstream job to have succeeded or skipped
        env:
          RESULTS: ${{ join(needs.*.result, ',') }}
        run: |
          echo "upstream results: $RESULTS"
          case "$RESULTS" in
            *failure*|*cancelled*) echo "::error::a required job did not succeed"; exit 1 ;;
          esac
